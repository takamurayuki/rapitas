# Confirmed false positives ignored by the gitleaks CI gate (Secret scanning).
# Format: <commit>:<file>:<rule-id>:<line> — one fingerprint per line, copied
# verbatim from the gitleaks finding output.
#
# task 501: mask.test.ts originally embedded a fake AWS-shaped access key to
# exercise the decision-trace masking regexes. The literal was defused (built
# at runtime) in 25e4a76a, but gitleaks scans each historical commit in a PR
# range, so the introducing commit keeps tripping the gate. History rewrite is
# not an option (the branch backs an open PR), hence these pins.
33b7a8731dd5e78ece85b56ae45293a2e05e4959:rapitas-backend/services/observability/decision-trace/mask.test.ts:aws-access-token:22
33b7a8731dd5e78ece85b56ae45293a2e05e4959:rapitas-backend/services/observability/decision-trace/mask.test.ts:aws-access-token:24
