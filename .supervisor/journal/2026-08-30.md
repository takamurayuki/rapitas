# 2026-08-30 監督ジャーナル

## 競合防止プラン 手順3: 競合解消タスクの完了条件に PR の mergeable を組み込む（L3）

- **観測**: #762「PR #534 の競合を解消」08:54 起票 → 09:0x `conflict_resolution_completed` → 09:20 時点で PR #534 は依然 `mergeStateStatus=DIRTY / mergeable=CONFLICTING` → 自動マージ監視が同一タスクを再起票。完了経路 `verify-commit-pr.ts` は verify.md 保存＝完了とみなし、GitHub の状態を一切見ていなかった。
- **仮説**: 「競合が解消された」の根拠は verify.md ではなく GitHub。DIRTY のまま完了すれば再起票ループになり、競合件数の計測（手順1・2 の効果測定）も汚れる。
- **介入 (L3, harness)**: 新規 `routes/workflow/handlers/file-save/conflict-pr-merge-state.ts` — `readConflictPrVerdict(taskId, prNumber)` が theme.workingDirectory で `readMergeState`（既存 `gh pr view --json mergeStateStatus`）を照会。UNKNOWN は 5s×3 再試行、gh 失敗/UNKNOWN 継続は fail open。`verify-commit-pr.ts` の競合解消分岐は DIRTY なら CAS 完了せず `attemptVerifyRepair` で implement→verify に差し戻し（理由に PR 番号と「base を取り込んで push」を明記）、予算枯渇時は blocked + 遷移 `conflict_pr_still_dirty`、stale なら何もしない。rapitas に直させなかった理由: 完了ゲート（保護パス）で、計測を直してから育てる原則。
- **検証**: `verify-commit-pr.test.ts` 9/9（新規 5: DIRTY→差し戻し／枯渇→blocked／stale→無操作／CLEAN→従来完了／PR番号なし→fail open）、`conflict-pr-merge-state.test.ts` 6/6、eslint/tsc クリーン。
- **結果（追跡）**: PR #534 は本記録時点でも DIRTY、#762 は todo で待機中。次回 #762 実行時に DIRTY のまま verify.md を保存すれば差し戻しが入るはず。効果指標: `conflict_resolution_completed` 直後に同 PR が再起票される件数（基線 08-30: 1/1）。

## 競合防止プラン 手順1: implementer 起動直前のファイル重複ホールド（L3）

- **観測**: 選定時の重複ゲート（task 573 B）は 3 日間で `task.deferred` 0 件。理由は直近週の auto_merged 83 件中 46 件が lightweight で、選定時点に plan.md が無く比較対象が空だから。7 日間の競合 3 件（#630 standard, #693/#759 lightweight）のうち #759 の research.md は衝突ファイル `log-health-suppressions.ts` を名指ししていた（#758 の PR #533 が open のまま 08:33 に #759 が選定され、#533 マージ 08:51 より前に同ファイルへ追記）。マージバリア（573 C, 既定 OFF）を ON にすれば防げたが、PR open→merge の中央値 17 分 × 12 件/日 ≈ 3.4h/日のアイドルで、競合率 5% に対して割に合わない。
- **仮説**: ファイルが判明する最初の時点＝implementer 起動直前（research.md または plan.md）で、他タスクの open auto-PR の変更ファイルと突き合わせて保留すれば、アイドルは重複時（≈5%）だけに限定できる。
- **介入 (L3, harness)**: 新規 `services/workflow/workflow-orchestrator-overlap-guard.ts` — `guardImplementOverlap` を `runAdvanceWorkflow` の preflight 直後に配置。plan.md（無ければ research.md）を `parsePlanFiles` で解析し、`getOpenAutoPrsForTheme`（自タスクの PR は除外）× `getPrChangedFiles`（60s キャッシュ）× `overlappingFiles` で重複判定。重複時は `{ success:true, skipped:true, held }` を返し runner が失敗扱いせず再キュー（10s poll）。上限は `MERGE_BARRIER_MAX_HOLD_MS`（30 分）で timeout 解放、解放後 30 分は再保留しない。イベント `task.implement_overlap_hold / _released`（理由 no_open_pr/no_files/no_overlap/timeout）。kill switch `RAPITAS_IMPLEMENT_OVERLAP_HOLD=off`。fail open。runner の再キューログは held 理由を出す（従来の「already running elsewhere」と区別）。
- **検証**: guard テスト 10/10、workflow-runner.test 9/9、eslint/prettier/tsc クリーン。
- **効果指標**: `task.implement_overlap_hold` 発生数と平均 holdMs、`auto_merge_conflict_filed` 件数/週（基線 3/週）。ホールドが週 10 件を超える or timeout 解放が多いなら閾値/対象を見直す。

## 事故: develop CI 赤 + ゴーストソケット再発（10:00–10:20）

- 手順3 のコミット d4d9e3fa は eslint/tsc のみで prettier を通しておらず「Check backend formatting」で develop が赤 → 4cbbd0b5 で整形。今後の監督コミットは prettier --check を必須手順に入れる。
- dev.js が push を検知して 10:14 にバックエンド自動再起動 → 旧 PID 3108 のソケットを孤児 `tail.exe -f /dev/null` が保持し LISTEN 2 本 → UI 永久ロード。孤児停止で復旧。L3 候補: dev.js 再起動後の「死んだ PID の LISTEN」検知と孤児保持者の報告。

## CI 赤の是正と行数制限ゲートの実体化（10:20–10:45）

- **観測**: PR #534 の赤 3 件 = Lint Code（私の未整形テスト、develop 経由で混入）/ Full Suite (Advisory)（`continue-on-error: true`、設計上ブロックしない）/ Enforce per-file line limits。行数制限は `DEFAULT_BLOCKING_CHECKS` に無く、PR は赤のままマージされ続けて develop に NEW 4 + GREW 5 の計 9 ファイルが 500 行超で蓄積（execute-route.ts 763, process-runner.ts 692, idea-box-service.ts 679, concern-backlog-service.ts 631, verify-self-repair.ts 611, agent-orchestrator.ts 587, index.ts 572, theme-auto-run-scheduler.test-support.ts 571, adversarial-diff-review.ts 559）。ゲートが飾りだった＝壊れた信号。
- **介入 (L2+L3, 0edd24ec)**: `--update-baseline` で現状を baseline に固定（全 PR を緑に戻す応急）＋ `Enforce per-file line limits (with ratchet baseline)` を auto-merge の必須チェックに追加。分割は rapitas に: 懸念 9 件（#888 #96 #101 #121 #3240 #813 #77 #83 #522、type=refactor、受入基準は差分で判定可能な形）。PR #534 は `gh pr update-branch` で base を取り込み、auto_merge_exhausted の駐機から head 変化で自動復帰させる。
- **監督側の再発防止**: 監督コミット前に `prettier --check` を必須化（今回 develop を赤にした原因）。ウォッチャーに `[ghost]`（:3001 の LISTEN が 2 本 or 所有 PID 死亡）を追加し再起動済み（Monitor b5x8n3brv）。
- **#755**: 3 回目の差し戻しで non-convergence ブロック → 原因は計画「新規ファイル 0」と実装の分割 3 ファイルの齟齬（判定は正しい）。L1: `POST /workflow/tasks/755/revise-plan` で計画に 3 ファイルを明記させる指示 → research_done へ。実装は変更不要と明記。

## 検証ゲートの CPU 負荷（運用者要望: テストを CI に逃がせないか → A 案で合意）

- **観測**: `automated-verifier.ts` が lint / tsc / `bun test --isolate` / prettier を `Promise.all` で同時起動。優先度制御なし。`bun test --isolate` 自体は逐次（`--parallel` 未使用）なので、山は「4 本同時＋tsc」。
- **判断**: CI 委譲（B）は open→merge 中央値 17 分を verify と self-repair の回数分だけ上乗せしスループット半減、かつ CI は既に第 2 ゲートとして機能。まずローカルで静かにする（A）。
- **介入 (L3)**: 新規 `quiet-verification.ts` — `spawnQuiet`（spawn 直後に `os.setPriority(pid, BELOW_NORMAL)`; Windows は子が優先度クラスを継承）と `runProjectChecks`（既定は lint+format → tsc → test の直列、`RAPITAS_VERIFY_PARALLEL=1` で旧挙動、`RAPITAS_VERIFY_QUIET=off` で優先度変更のみ無効）。`automated-verifier.ts` は行数中立（1002 行、ratchet 維持）。テスト 6 + 既存 79 pass。
- **効果指標**: verify 中のホスト CPU ピーク（タスクマネージャ目視 / `[cpu]`）と verify 所要時間の中央値（+1〜2 分を許容）。悪化なら `RAPITAS_VERIFY_PARALLEL=1` で戻せる。

## CPU 追加施策 1+2（運用者合意: 1→2→3 の順）

- **実測 (60s, 4 コア)**: claude CLI 23.2s、WebView2 計 ≈20s、Taskmgr 6.7s、bun backend 4.5s、Next dev 1.1s。定常で機械の約 23%。
- **介入 1 (L3)**: `services/agents/process-priority.ts` — `spawnLowPriority` / `lowerProcessPriority(pid, flag)`。claude / codex / gemini の runner と `claude-code-stream`（補助 AI `--print`）の spawn を差し替え（`RAPITAS_AGENT_QUIET=off` で無効）。`quiet-verification.ts` は同モジュールへ委譲（単一実装）。Windows は子が優先度クラスを継承するので、エージェントが自分で回す bun test / tsc / git も低優先度になる。
- **介入 2 (L3)**: `juror-scheduling.ts` — 陪審 3 名を既定で逐次（`RAPITAS_JURY_PARALLEL=1` で従来）。全員に必ず聞く契約は維持。`adversarial-diff-review.ts` は行数中立（559）。
- **3 は rapitas に**: 懸念 #7866（UI の 1s タイマー/重複ポーリングを可視性連動で停止）。
- **検証**: 新規 12 テスト + 既存 13 ファイル pass、tsc 0、ratchet OK。**効果指標**: UI 操作中の体感（固まらないか）、verify 所要時間中央値、完了時のピーク CPU。

## 自動実行が無言で停止していた（10:24 頃〜11:05、L0→L2）

- **観測**: PR #533/#534/#762 は auto-merge 済み・develop CI 全緑。一方 #755 が plan_approved のままキュー 0 で 20 分以上停滞。原因は `ThemeAutoRun.enabled=0` — スケジューラ自体が死んでいた。今日のログに stopAutoRun 系の行は皆無で、いつ・誰が enabled を落としたか特定不能（候補: 10:24 の self-deploy 再起動時に status='stopping' を踏んだ recoverOnStartup の無言掃除。`finalizeStop` とこの掃除はどちらもログを書かない）。
- **介入 (L2)**: `POST /themes/1/auto-run {action:start}` で再開（enabled=1, running）。監督ウォッチャーは enabled=1 のテーマしか stall を見ていなかったため盲点だった → `[autorun] theme N enabled 1->0` フリップ検知を追加して再起動（Monitor bbypjr4yc）。
- **L3 候補（次サイクル、1変数ずつ）**: (1) `finalizeStop` / recoverOnStartup の 'stopping' 掃除に必ずログを書く（帰属可能にする）。(2) self-deploy 再起動では enabled を保存し復元する（再起動が自動実行を殺してはならない）。(3) `[ThemeAutoRunScheduler] Task N was already queued; tracking it` の 12 秒毎 WARN ノイズ抑制。

## overlap ホールド初発火の是正（運用者のログ報告より、11:30–11:50）

- **観測**: ゲートは #755 を正しく検知したが、(1) 保留相手が **stale PR**（#435=643 由来・#467=671 由来、何週間も open のまま）で 30 分待っても無意味、(2) runner が 10 秒毎に INFO でスパム、(3) 11:14→11:30 の無言ハングは `getPrChangedFiles` の `execAsync` に **timeout が無く**、boot 後最初の gh がブロックした（16 分）と符合。
- **介入（同一機能の是正、1 コミット）**: gh exec に 15s cap（失敗は既存の fail-open へ）／`OVERLAP_PR_MAX_AGE_MS=6h` — createdAt が 6 時間以内の PR だけがホールド根拠（中央値 17 分でマージされる健全 PR のみを待つ）／held 再キューの毎 tick ログを debug 化（ホールド開始の INFO は guard 側に既存）。テスト: guard 11、cache 9、runner 9 pass。
- **残課題**: stale なオープン PR が 15 本近く滞留している（#435/#467 等）。競合とホールドの種なので、棚卸し（クローズ or 再着地）を別途起票候補に。

## #755 の顛末: 計画改訂の黙殺と settle 救済（11:39–11:55、L3）

- **観測**: 10:07 の `revise-plan` 指示は `plan_revision_requested` 遷移として記録されたが、直後の `artifact_reuse_fastforward` が既存 plan.md を再利用して **planner をスキップ** → ジャッジには旧計画（新規0）が渡り 3 度目の FAIL（non-convergence 再発火）。ブロック寸前で `VerifySettle` が「PR #537 が既に存在」を根拠に完了へ回復（コードへの異議は 3 回とも無し — 実体として妥当）。その後 PR #537 の CI 赤で ci_repair が回っている（正常ループ）。
- **介入 (L3, 3c09d219 の次)**: `artifact-reuse-reconciler.ts` — `getPendingPlanRevision` が未消化の改訂指示を返す間は plan.md を再利用可能とみなさない（planner が指示付きで必ず再走する）。回帰テスト追加（12 pass）。
- **教訓**: 改訂指示の「pending」判定は plan.md の updatedAt との時刻比較で既に存在していた — 消費側（planner 文脈）だけが知っていて、再利用側が知らなかった。ゲート同士の情報分断が原因。

## 行数ゲート初仕事: PR #537 を正しく拒否（12:0x）

- #755 の PR #537 は上限超過ファイル 2 件（verify-self-repair.ts 611→621 / theme-auto-run-scheduler.test-support.ts 571→578）をさらに太らせるため ratchet が FAIL → auto-merge 枯渇駐機。ゲートの意図どおりの動作。
- 対応: baseline を緩めず、分割懸念 #522 / #83 を severity=high に昇格して先に着地させる。分割 merge → #537 は head 変化で自動復帰（既存の枯渇駐機解除機構）。ci_repair のエージェントが「太らせず分割する」判断をできるかは要観測 — できなければ ratchet の FAIL 理由を ci_repair のプロンプトに渡す L3 を検討。

## api-client timeout のコンソール洪水を抑制（運用者報告、13:2x）

- **観測**: `[api-client] timeout` が素の `logger.error` で、可変の elapsedMs を引数に含むため既存の throttle も効かない形だった。今日の頻発は監督デプロイによる再起動 ~8 回で in-flight GET が一斉に 15s タイムアウトしたのが主因。
- **介入 (frontend 1行)**: `client.ts` の該当ログを `errorThrottled('[api-client] timeout', requestLabel)` に変更（エンドポイント単位で 5 秒窓、再起動中は既存ゲートで全抑制）。経過時間は abort メッセージに残る。vitest 57 pass。

## 再起動直後の無言ハング — 容疑者を消し込み、計器を設置（13:3x–13:5x）

- **消し込み**: 実行ロックはインメモリ非ブロッキング（白）／probe は 3s×3+12s（白）／verify-settle は再起動後 60s に落ちる（単独では 16 分を説明できず）／gh は 15s cap 済み／ハング中の backend に子プロセス無し（in-process の未解決 await）。「Already running」は 00:07 から出ており私のゲート以前からの現象。GET /tasks/764 の 15s タイムアウト（運用者報告）はこのハング窓と boot が重なった瞬間のもの。エンドポイント自体は 20–30ms。
- **介入 (L3, 計器)**: `raceWorkflowAdvance` 入口と `runPreflight` 完了に INFO パンくずを追加。次のハングで「preflight 内 / guard〜prepare / それ以降」を一発で二分できる。
- 次の再起動（この push で発火）以降のハングを待ち、パンくずで確定診断 → 恒久修正。

## #764 停滞の原因と是正: overlap ホールドの循環待ち（運用者報告、13:5x）

- **原因の連鎖**: (1) 13:24 の計画承認後、私の push 起因の dev-restart が 13:24/13:34/13:44 と 3 連続 → 各 boot の初回 advance ハング＋保留時計（holdSince, インメモリ）のリセット。(2) その上で overlap guard が「PR #537（2h前=fresh）が verify-self-repair.ts を変更中」として #764 を保留 — **#537 を通すための分割タスクが #537 を待つ循環**。cycle log の task.implement_overlap_hold で確認。
- **介入 (L3)**: guard に isParked 条件を追加 — linked task の直近が `auto_merge_exhausted`（駐機）の PR は「もうすぐマージされる PR」ではないので待たない。#537 は駐機中のため保留対象から外れ、#764 は即時実装へ。回帰テスト追加（12 pass）。
- **教訓**: 「fresh なら待つ価値がある」は不十分 — マージ可能性（駐機でないこと）まで見る。保留時計が再起動でリセットされる点は既知の弱点として残す（駐機除外で実害は解消）。

## 午後の運用記録（14:30–16:00）

- **CPU 続報（運用者報告2件）**: 実測で暴走 vitest ワーカー（私の 13:27 の診断コマンドの残骸、74 CPU 分）を特定し停止 — 以後、背景化したテストは必ず後始末する。残る消費は「エージェント実行そのもの」（低優先度化済み）と WebView2（#7866 起票済み）と Taskmgr 自身。
- **#768（収束条件診断）**: 回答で「修正済み前提＋残価値」へ誘導 → 診断は良質で、VerifySettle が陪審 FAIL を上書きする残存ギャップを特定（#755 の実例と一致）。実装は #772 として起票（方針 A 固定）。
- **UX 起票**: #771 自動再起動時のブロッキングモーダル（手動時のストア/モーダル再利用、SSE restarting イベント + health フォールバック）。
- **PR #537**: 枯渇駐機は「PR 自身の head 変化まで手動待ち」で base 前進では復帰しない仕様と確認 → 手動エンドポイント `/github/pull-requests/:id/resolve-conflicts` で競合タスク #773 起票（competing 2 ファイル＝分割対象そのもの）。改善候補: 駐機 PR の DIRTY 検知→自動 resolve-conflicts。
- **PR #542（#769）も行数ゲートで駐機** — ci_repair がゲートを直せないパターン 2 例目で閾値到達 → **L3**: ci-self-repair の指示に「line limits 失敗時は `node scripts/check-large-files.cjs` をローカル実行して NEW/GREW を特定し分割せよ（baseline 書き換え禁止）」を条件注入（末尾50行抜粋では ← GREW 行が切れて届いていなかった）。

## チェーン完結: PR #537 マージ（17:4x）

ratchet 正当拒否 → 分割 #764/#765 優先着地 → 競合タスク #773（ci_repair への ratchet 指示注入後の初周回）→ mergeable ゲート通過（conflict_resolution_completed、2例目・正当）→ 駐機 auto-merge が head 変化で復帰 → auto_merge_blocked(CI待ち)→ マージ。本日の監督機構（手順1/3・ratchet 必須化・分割誘導）が一気通貫で機能。#771（自動再起動モーダル）もマージ済みで次回再起動から有効。
残る観測: #772（settle フレッシュ拒否ガード）と #769/PR #542（head 変化待ち駐機 — 新 ratchet 指示での ci_repair 再挑戦は #542 の head が動く契機がないため、次に #542 相当が出た際に効果測定）。競合週次基線: 3 件/週 → 効果は来週の auto_merge_conflict_filed 件数で判定。

## 効果測定の基線固定（sup-metrics.py、2026-08-30 18:0x）

- **7日**: merged 92 / 完了タスク 134 / verify_repair 100回(45タスク, 差し戻し率34%) / ci_repair 12 / 非収束 15 / 競合 4 / 修正不要 29 / 駐機 16 / リードタイム中央値 57分
- **24h**: merged 31 / 完了 51 / 差し戻し率 20% / 競合 2 / 修正不要 17 / リードタイム中央値 35分
- **次の一手ごとの判定指標**（各介入から 1 週間の 7d 値で判定）:
  1. 無言ハング根治 → 「Already running が 60 分以上連続する窓」= 0/週（パンくずで確定診断後に修正）
  2. 誠実性ゲート構造化 → verify_repair のうち「ゲート誤爆起因」= 0（現状は都度 regex 補修 9 形状）、差し戻し率 34%→25% 以下
  3. 賢さ指標ダッシュボード → 本スクリプトの KPI を UI 化し、rapitas 自身が回顧ジョブで参照すること
- 24h の差し戻し率 20% は 7d 平均 34% を下回っており、本日の一連の介入（改訂黙殺修正・ゲート整合・ratchet 指示注入）が効いている**初期シグナル**。ただし 1 日分なので判定は 4 サイクル連続低下（卒業基準）で行う。

## 通知の生キー表示（運用者報告、18:4x）

- #763 の実装バグ: backend は完全パス `notification.types.x.title` を保存、フロントは `notification` 名前空間スコープの t にそのまま渡して二重接頭辞 → next-intl は未解決キーで例外を投げずキー文字列を返すため、フォールバックも発動せず生キーが表示された。
- 修正（frontend 直接、3 テスト付き）: キーの先頭 `notification.` を剥がして解決し、それでも未解決（結果が .title/.message を含む）なら保存文字列へフォールバック。レガシー行の挙動は不変。
- 教訓 → 陪審は「キーが fragment に存在するか」までは検証したが「名前空間の掛け合わせで実際に解決するか」は差分から判定できない。受入基準に「実レンダリングのスナップショット/単体テスト」を要求すべきだった（#763 起票時の私の抜け）。

## #774 ブロックの根本原因と是正: 設定消失時の auto-commit フォールバック穴（19:0x–19:3x）

- **連鎖**: 私の push→再起動で #774 の実装実行が孤児化 → stale 回復は正しく巻き戻したが **AgentExecutionConfig 行が消失** → 再実行後の auto-commit が `execConfig?.workingDirectory || theme.workingDirectory` で **primary にフォールバック** → worktree ガードが正当拒否 → PR 未作成で verify_pr_not_created ブロック。成果物は `.worktrees/task-774-*` に無傷。ガード網は機能、穴はフォールバック順（worktree-isolation の既知ジャンル 2 例目）。
- **介入 (L3)**: 新規 `routes/workflow/commit-cwd.ts` — 解決順を「明示 cwd → タスクの worktree（`.worktrees/task-<id>-*` 実在検査）→ テーマ dir」に。workflow-auto-commit.ts は行数中立（503 維持）。テスト 5+既存 4 pass。
- **残対応**: push 後の再起動で新コードが載ってから #774 を retry（worktree でコミット→PR 作成の見込み）。
