# 2026-08-31 監督ジャーナル

## #784 の wall budget 誤爆（01:49、L0→L2）

- #784 が 45 分 wall budget で強制停止 → blocked。ただし大半は #785 の ci_repair 裏での**キュー待ち時間**で、実行時間ではない。待ち時間を wall 予算に算入する設計問題（改善候補: wall 時計はフェーズ実行開始で起動 or 待機中は停止）。retry で復帰。

## ゴーストソケットの因果連鎖を完全特定（02:0x）

- **連鎖**: 検証ゲートが worktree で `bunx vitest` 等を spawn（shell:true → cmd 経由でハンドル継承）→ その最中に dev-restart が「エージェント無し」を見て backend を kill → 死んだ backend の :3001 listen ソケットを検証子プロセスが継承保持 → LISTEN 二重化・health 000・UI 永久ロード。今回は #785 の検証 vitest ツリー（01:50:52 起動、親 2860 は 01:51 死亡）が保持者で、kill で即復旧。
- 今日 3 回の [ghost] はすべてこの型（保持者は tail.exe / vitest 等、いずれも死んだ backend の生存中に生まれた孤児）。
- **恒久策候補（L3、次サイクル最優先）**: dev-restart / AutoRestartMergedCode の静止点判定に「アクティブな検証子プロセス無し」を追加する（現状は agent 有無のみ）。併せて検証 spawn の handle 継承遮断（bun/Node の windowsHide では不十分）を調査。
- ウォッチャーの [ghost] 検知 → プレイブック（孤児列挙 → inert kill）は 3 連続で機能。

## 朝の後始末（05:0x–06:2x）

- **PR #553/#555 の駐機解消**: #553 は react-window 依存がブランチ未同梱（メインで install する規約とブランチへの package.json 反映の断絶）→ 依存をブランチへ移植。#555 はカバレッジ床 8.99% vs 9% — 同梱テストが CI のハードコード一覧に無く分母だけ増えていた → 一覧に追加（エージェントは workflows 不可のため監督者専任）。#555/PR マージ済み、**#784（アイドルタイマー）本線着地**。
- **誤コミット事故（未 push で完全復旧）**: prune 済み worktree 残骸ディレクトリの git 文脈が primary に解決し、`git add -A` が primary の未コミット変更を develop に巻き込んだ → reset で取消し。教訓: worktree での操作前に `git rev-parse --show-toplevel` 確認、残骸は即削除。
- **#787（陳腐アラーム）**: アンカーが description 側のみでタイトル限定パーサを素通し → cancel ＋ parseSelfDetectSignature を detail 対応に拡張（テスト 9 pass）。
- **朝の L3 キュー**: ①再起動静止点に検証子プロセス判定 ②wall 時計の待機時間除外 ③CI フロントテスト一覧のハードコード廃止 ④prune 残骸の即削除 ⑤watcher の stall 盲点（別タスク実行中）。

## L3 ①: 自動再起動の静止点に「補助子プロセス無し」ゲートを追加（06:4x）

- process-priority に生存子プロセス追跡（spawn で登録・exit/error で解除、`countActiveHelperChildren`）を追加し、dev-restart の gate 列に helper-children-running を挿入（import 失敗時は fail open — テストの部分モックにも安全）。検証コマンドとエージェント CLI が生きている間は self-deploy 再起動を延期し、ゴーストソケットの発生源（socket 継承した子の孤児化）を根から断つ。テスト: process-priority 4 / dev-restart 6 / quiet-verification 6 pass。
- 効果指標: `[ghost]` 検知件数/日（基線: 08-30〜31 で実害 2 件＋一過性 4 件）→ 0 目標。

## L3 ②: wall budget の待機時間誤爆修正（08:5x）

- 新規 `queue-wait-exemption.ts` — 「live heartbeat あり」または「自分の項目が queued かつ他タスクの項目が running（＝順番待ち）」なら hang backstop の対象外。advance-active は行数中立（451 維持、hasLiveExecution 呼び出しを liveOrQueuedBehind に差し替え）。本物のハング（誰も running でない滞留）は従来どおり停止する。テスト: exemption 5 / advance-active 31 pass。
- 効果指標: `task.hang_backstop`（cause=wall_budget_exceeded）の誤爆件数（基線: 08-31 未明に 3 件）→ 0 目標。

## 半デプロイ事故と復旧（09:0x–09:4x、運用者報告 2 件）

- **症状**: タイムライン UI がデータ空＋internal server error。**因果**: PR #553 マージ後、フロントは HMR で新 UI 即反映・バックエンドは旧コード（helper-children ゲートが再起動を正しく延期）→ 新 API 404 の「半分だけデプロイ」。管理 API でフェーズ境界再起動 → API 200。その後、私が稼働中 dev サーバー配下で .next を削除し 500 を悪化（dev.js が直前にフロント自動再起動していたのを見落とし）→ フロントツリー停止 → dev.js 再生成で完全復旧。
- **教訓**: (1) フロント/バックの整合は「マージ＝デプロイ」ではない — 再起動完了までは半デプロイ。(2) .next 削除は必ずフロントプロセス停止後。(3) helper-children ゲートは意図どおり動作したが、「延期しすぎ」を可視化する diag は今日既に出ている（helper-children-running）— 長時間延期時の強制点（例: 30 分超で次のフェーズ境界に必ず再起動）を検討。
- **見た目退行**: ターミナル調・ヘッダー喪失 → #796 起票（high）。
