# 2026-08-29 — 監督モード初回セッション

## 観測

### §4 初期タスクの検証結果（すべて実データで確認）

| # | 項目 | 実態 | 判定 |
| --- | --- | --- | --- |
| 1 | 進化指標がゲート差し戻しを completed 計上 | `prompt-evolution-runner.ts` は `ROLE_TROUBLE_CAUSES`（`role-evidence.ts:69`）で差し戻しを failure 側に補正済み。runner は `behavior-scheduler.ts:118`（月曜07:00）で起動、PromptEvolution 19行（週次5行ずつ）、15行に候補プロンプト生成済み。承認済み(3行)は `workflow-orchestrator-context.ts:42` `getApprovedRoleAddendum` でロールプロンプトに注入される — ループは閉じている | **完了済み**（前提が古い） |
| 2 | reviewer が到達不能 | reviewer ロールは 2026-08 に**廃止**（`workflow-types.ts:14`、plan-review は phase-critic が担う） | **前提消滅** |
| 3 | auto_verifier が進化対象外 | runner の roles 配列に `auto_verifier` あり。PromptEvolution に `workflow_role_auto_verifier` 行あり | **完了済み** |
| 4 | implementer プロンプトの乖離 | `.claude/CLAUDE.md:288`「plan.md に無いファイルは即停止」 vs ゲート実態: scope は ADVISORY（task 298 で降格、`automated-verifier.ts:862`）、HARD は `PROTECTED_PATH_RE`（:736）の保護パスのみ | **実在** → 本日 L3 |
| 5 | ストリーミング二重チャンク | AgentExecutionLog の (executionId, sequenceNumber) 重複 0件、24h 同一 chunk 重複 0件 | 再現せず |
| 6 | claudeSessionId / --resume 未接続 | `adapter-execution.ts:35,60` で `resumeSessionId` に配管済み | **完了済み** |
| 7 | worker 再起動時の running 行リーク | 23:35 再起動後の非終端 agentExecution 0件。LeaseSweep + RecoveryManager が起動時に回収 | **完了済み** |

§4-1〜3 が完了しているため、「成長」評価の禁止は §4-4 の完了をもって解除される。

### §4-1 の残余（観測のみ）

`ROLE_TROUBLE_CAUSES.implementer` に `verify_repair_non_convergence` / `verify_pr_not_created` が無い。ただし同一タスクに先行する `verify_repair` 行が同じ窓に必ず存在するため、実質の取りこぼしは無い。7日窓のサンプル: implementer 180 / researcher 124 / auto_verifier 95 / verifier 90 / planner 35。

### 進化ループの未消化

PromptEvolution: approved 3 / proposed 12 / pending 4。experimentId は全19行 null（Experiment 接続経路は未使用）。**12件の提案が人間の承認待ち**（`/system-prompts`）。これは欠陥ではなく運用。承認されなければ進化は反映されない。

### 監督モード移行前（本日 00:00〜23:35）の介入 — 基線として記録

移行前に L3 を連続投入しており **「1介入=1変数」に反する**。効果判定は個別にはできないため、束として前後比較する。

| commit | 内容 | 層 |
| --- | --- | --- |
| 788198c1 | 引用タイトル由来の受入基準混入を検出（161件中2件、両方本物） | ハーネス |
| df534e1d / 90413b06 | 誠実性ゲートの誤判定3形（28件中5件反転） | ハーネス |
| cf1cf04d | ログ由来タスクに仕様テンプレート | 起票 |
| da92917f | 完了済みタスクの質問を再通知しない | 検出器 |
| 5c4ed609 | ジャッジの「未変更＝未着手」推論を禁止（674 で前後比較確認済み） | ハーネス(判定側) |
| a3cc3ca2 | ログ由来の起票に生ログ行・回数・時刻を載せる | 起票 |
| ab0116d6 | 仮説証拠 API の指示文を送信可能な JSON に | 仕様乖離の是正 |
| 3e81f32f | 起きていない runner 再起動を78回報告していた | 計測系 |
| 7887bf5a | 連続タイムアウトするジャッジを休ませる | ハーネス |
| 5f4e3718 / 2a56811e | 壊れた Prisma 生成物を起動時に検出・ホット再起動でも再生成 | 基盤 |

うち **5c4ed609 と ab0116d6 はロールに届くプロンプト文言の変更**であり、監督モードの禁止事項（ロールプロンプト直接書換）に形式上抵触する。前者はジャッジ（判定側ハーネス）、後者は §4-4 と同型の仕様乖離（送れない JSON）として L3 相当と整理するが、以後は同種の変更を L1 経由にする。

### 基線（2026-08-27 14:30 時点、48h）

verify_repair 27 / verify_repair_non_convergence 6 / adversarial_review_failed — / intake_question_answered 8 / blocked_escalated 8 / verify_passed 30 / auto_merged 23。

本日 24h（08-28）: 起票 39 / 完了 42 / auto_merged 34 / verify_repair 26 / non_convergence 10 / adversarial_review_failed 9 / verify_no_change_confirmed 8（完了の 19%、うち 4 件がログ由来の情報不足）。

### 本日の障害

10:00 に dev.js 外からの `prisma generate` が生成物のみを壊し（`cpuTimeMs` 列、スキーマにも DB にも無い）、13 時間・620 回の失敗。キャッシュ検証が入力しか見ず、ホット再起動に生成ステップが無かったため再起動で回復しなかった。23:35 のフル再起動で復旧。blocked 6→1（#715 は正当なブロック）。

## 仮説

- §4 の前提は 2026-08 前半の調査に基づいており、rapitas 自身の自己開発で 5 項目が解消された。監督の物差しも定期的に再検証が必要。
- `verify_no_change_confirmed` の高さ（19%）は起票の情報不足が主因。a3cc3ca2 で改善するはず。
- 二重記録（#710 の主題）は `cutoffRecorded` を見ない呼び出し元 2 箇所が残っており、遷移カウント（non_convergence / adversarial_review_failed）を **二重に膨らませている**。指標の分母・分子に影響するため、#710 の完了を待って再集計する。

## 介入

- レベル: **L3**（§4-4）
- 内容: `.claude/CLAUDE.md` Step 4 の停止条件 1 行を、ゲート実態（scope=ADVISORY、保護パスのみ HARD）に合わせて書き換え。他は触らない。
- なぜ rapitas 自身に直させなかったか: CLAUDE.md はリポジトリの仕様であり rapitas の自己改善ループの外側。ループ内から自分への指示を書き換えさせると、ゲートを緩める方向のバイアスが入る（本日 #669 で検出しきい値を緩める提案を観測済み）。
- 変数は 1 つ。同日に他の L2/L3 を入れない。

## 結果

- 次セッションで検証すること:
  1. §4-4: verify.md に「計画外だが実施した」記録が現れ、かつ停止報告が出ないこと（14 日基線: 停止 0 件、防御的説明 4 件）。
  2. 移行前の束の効果: `verify_repair` / `intake_question_answered` / `verify_no_change_confirmed` の 48h 値を基線と比較。
  3. #710（二重記録ガード）完了後、遷移カウントを再集計し、指標の膨らみ分を明記する。
  4. #715 の 3 回目リトライを観測（肩代わりしない）。
