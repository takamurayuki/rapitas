# 2026-08-29 — 監督モード初回セッション

## 観測

### §4 初期タスクの検証結果（すべて実データで確認）

| # | 項目 | 実態 | 判定 |
| --- | --- | --- | --- |
| 1 | 進化指標がゲート差し戻しを completed 計上 | `prompt-evolution-runner.ts` は `ROLE_TROUBLE_CAUSES`（`role-evidence.ts:69`）で差し戻しを failure 側に補正済み。runner は `behavior-scheduler.ts:118`（月曜07:00）で起動、PromptEvolution 19行（週次5行ずつ）、15行に候補プロンプト生成済み。承認済み(3行)は `workflow-orchestrator-context.ts:42` `getApprovedRoleAddendum` でロールプロンプトに注入される — ループは閉じている | **完了済み**（前提が古い） |
| 2 | reviewer が到達不能 | reviewer ロールは 2026-08 に**廃止**（`workflow-types.ts:14`、plan-review は phase-critic が担う） | **前提消滅** |
| 3 | auto_verifier が進化対象外 | runner の roles 配列に `auto_verifier` あり。PromptEvolution に `workflow_role_auto_verifier` 行あり | **完了済み** |
| 4 | implementer プロンプトの乖離 | `.claude/CLAUDE.md:288`「plan.md に無いファイルは即停止」 vs ゲート実態: scope は ADVISORY（task 298 で降格、`automated-verifier.ts:862`）、HARD は `PROTECTED_PATH_RE`（:736）の保護パスのみ | **実在** → 本日 L3 |
| 5 | ストリーミング二重チャンク | AgentExecutionLog の (executionId, sequenceNumber) 重複 0件、24h 同一 chunk 重複 0件 | 再現せず |
| 6 | claudeSessionId / --resume 未接続 | `adapter-execution.ts:35,60` で `resumeSessionId` に配管済み | **完了済み** |
| 7 | worker 再起動時の running 行リーク | 23:35 再起動後の非終端 agentExecution 0件。LeaseSweep + RecoveryManager が起動時に回収 | **完了済み** |

§4-1〜3 が完了しているため、「成長」評価の禁止は §4-4 の完了をもって解除される。

### §4-1 の残余（観測のみ）

`ROLE_TROUBLE_CAUSES.implementer` に `verify_repair_non_convergence` / `verify_pr_not_created` が無い。ただし同一タスクに先行する `verify_repair` 行が同じ窓に必ず存在するため、実質の取りこぼしは無い。7日窓のサンプル: implementer 180 / researcher 124 / auto_verifier 95 / verifier 90 / planner 35。

### 進化ループの未消化

PromptEvolution: approved 3 / proposed 12 / pending 4。experimentId は全19行 null（Experiment 接続経路は未使用）。**12件の提案が人間の承認待ち**（`/system-prompts`）。これは欠陥ではなく運用。承認されなければ進化は反映されない。

### 監督モード移行前（本日 00:00〜23:35）の介入 — 基線として記録

移行前に L3 を連続投入しており **「1介入=1変数」に反する**。効果判定は個別にはできないため、束として前後比較する。

| commit | 内容 | 層 |
| --- | --- | --- |
| 788198c1 | 引用タイトル由来の受入基準混入を検出（161件中2件、両方本物） | ハーネス |
| df534e1d / 90413b06 | 誠実性ゲートの誤判定3形（28件中5件反転） | ハーネス |
| cf1cf04d | ログ由来タスクに仕様テンプレート | 起票 |
| da92917f | 完了済みタスクの質問を再通知しない | 検出器 |
| 5c4ed609 | ジャッジの「未変更＝未着手」推論を禁止（674 で前後比較確認済み） | ハーネス(判定側) |
| a3cc3ca2 | ログ由来の起票に生ログ行・回数・時刻を載せる | 起票 |
| ab0116d6 | 仮説証拠 API の指示文を送信可能な JSON に | 仕様乖離の是正 |
| 3e81f32f | 起きていない runner 再起動を78回報告していた | 計測系 |
| 7887bf5a | 連続タイムアウトするジャッジを休ませる | ハーネス |
| 5f4e3718 / 2a56811e | 壊れた Prisma 生成物を起動時に検出・ホット再起動でも再生成 | 基盤 |

うち **5c4ed609 と ab0116d6 はロールに届くプロンプト文言の変更**であり、監督モードの禁止事項（ロールプロンプト直接書換）に形式上抵触する。前者はジャッジ（判定側ハーネス）、後者は §4-4 と同型の仕様乖離（送れない JSON）として L3 相当と整理するが、以後は同種の変更を L1 経由にする。

### 基線（2026-08-27 14:30 時点、48h）

verify_repair 27 / verify_repair_non_convergence 6 / adversarial_review_failed — / intake_question_answered 8 / blocked_escalated 8 / verify_passed 30 / auto_merged 23。

本日 24h（08-28）: 起票 39 / 完了 42 / auto_merged 34 / verify_repair 26 / non_convergence 10 / adversarial_review_failed 9 / verify_no_change_confirmed 8（完了の 19%、うち 4 件がログ由来の情報不足）。

### 本日の障害

10:00 に dev.js 外からの `prisma generate` が生成物のみを壊し（`cpuTimeMs` 列、スキーマにも DB にも無い）、13 時間・620 回の失敗。キャッシュ検証が入力しか見ず、ホット再起動に生成ステップが無かったため再起動で回復しなかった。23:35 のフル再起動で復旧。blocked 6→1（#715 は正当なブロック）。

## 仮説

- §4 の前提は 2026-08 前半の調査に基づいており、rapitas 自身の自己開発で 5 項目が解消された。監督の物差しも定期的に再検証が必要。
- `verify_no_change_confirmed` の高さ（19%）は起票の情報不足が主因。a3cc3ca2 で改善するはず。
- 二重記録（#710 の主題）は `cutoffRecorded` を見ない呼び出し元 2 箇所が残っており、遷移カウント（non_convergence / adversarial_review_failed）を **二重に膨らませている**。指標の分母・分子に影響するため、#710 の完了を待って再集計する。

## 介入

- レベル: **L3**（§4-4）
- 内容: `.claude/CLAUDE.md` Step 4 の停止条件 1 行を、ゲート実態（scope=ADVISORY、保護パスのみ HARD）に合わせて書き換え。他は触らない。
- なぜ rapitas 自身に直させなかったか: CLAUDE.md はリポジトリの仕様であり rapitas の自己改善ループの外側。ループ内から自分への指示を書き換えさせると、ゲートを緩める方向のバイアスが入る（本日 #669 で検出しきい値を緩める提案を観測済み）。
- 変数は 1 つ。同日に他の L2/L3 を入れない。

## 結果

- 次セッションで検証すること:
  1. §4-4: verify.md に「計画外だが実施した」記録が現れ、かつ停止報告が出ないこと（14 日基線: 停止 0 件、防御的説明 4 件）。
  2. 移行前の束の効果: `verify_repair` / `intake_question_answered` / `verify_no_change_confirmed` の 48h 値を基線と比較。
  3. #710（二重記録ガード）完了後、遷移カウントを再集計し、指標の膨らみ分を明記する。
  4. #715 の 3 回目リトライを観測（肩代わりしない）。

## 追記（セッション後半）

### 障害の真因が確定

`cpuTimeMs` は **task 714**（PR #507、commit 0edae828）が `prisma/schema/agents.prisma` と `schema.desktop/agents.prisma` に追加した列。10:00 の `prisma generate` は 714 の worktree から実行され、共有 `generated/` を書き換えた。CLAUDE.md §1 が禁じている操作そのもの。714 は 08-29 に develop へマージ済みのため、次回フル再起動で `db push` が列を作り不整合は解消する。

### primary checkout の未コミット変更が自動更新を止めている（L0）

`execution-resume.ts` / `resume-helpers.ts` / `branch-name-generator.ts` ほか 5 ファイルがセッション開始時（08-27）から未コミットのまま。`[AutoRestartMergedCode] Working tree dirty — skipping pull/restart` が本日 2 回。監督の commit も primary からは push できず、一時 worktree 経由で cherry-pick して push した。**誰の WIP か不明のため触らない。** 運用者に判断を求める。

### 再起動後（13:4x〜）

- 再起動後、schema / 生成クライアント / DB とも `cpuTimeMs` 無しで整合（primary は未コミット変更により 714 を未取り込み。取り込み時に再起動が要る）。
- **theme 1 の自動実行が 08-28 11:42 JST（障害の最中）から enabled=false だった。** 停止操作のログ痕跡なし（actor 不明）。2 時間遷移ゼロの原因。運用者の承認を得て 13:5x に再有効化（L2 相当の設定変更だが、育成介入ではなく運用状態の復元として扱う）。
- #707（[回顧] ゲート管轄逸脱）の intake 質問に operator として回答。主題は #710 と同じ二重記録のため、#710 完了後 24h の再発 0 件確認に限定し、ゲート条件の変更・#710 スコープとの重複を禁止。#710 未完了のうちは進めない依存を明記。
- L0 監視を常駐化（`sup-watch.py`）: 非定常遷移 / blocked 件数変化 / backend ERROR を 60 秒毎に差分通知。

### 16:0x #718 — 監督者の誤操作と復元

- #718 の実装フェーズ質問（plan.md の設計判断 vs 受入基準の矛盾）に **推奨 A** で回答。根拠: execution #3139 completed → 8ms 後に session #3156 failed、現在の終端セッション配下アクティブ実行 0 件。直すべきデータは無く、受入基準側（検出瞬間の状態を固定）が誤り。基準2 は #715 からの混入（**本日3件目**: #669/#717/#718、すべて自己検出ウォッチャー起票分）。
- **誤操作**: `answer-question`（インテーク用、回答後に plan.md をアーカイブし draft へ）を使ったため、承認済み plan が消えた。実装フェーズ発の質問は `resume-from-question`（previousStatus へ復帰）を使うべきだった。実装者の成果は worktree ブランチ `9cf729ab` に残存。
- 復元: アーカイブ版 plan（WorkflowFileVersion 23855B）を再保存 → research 再利用 fast-forward → plan_created → 自動承認。ハッシュ一致確認済み。
- #707 は「修正不要」完了。ただし指示した「#710 完了後 24h の再発 0 件確認」を待たずに結論（依存指示の不履行、L0）。完了直後に `gh pr create` が失敗 — **変更なし完了が PR 経路に入る**（#699/#700 と同型、反復 2 回目以上）。次サイクルで L3 候補。
- #719 完了（verify_passed）。障害でブロックされた 4 件のうち初の完走。
- 運用者要望「質問に推奨選択肢を示す」をアイデア #7394 として起票（ループ内側のプロンプト変更のため L1 経由）。監督者としては今後、質問回答の冒頭で「推奨: X + 根拠」を必ず示す。

### 16:3x L3（2件目）— 誠実性ゲート 6・7 形目

- #718 が「3156 failed | ❌」で差し戻され、UI に約3000件のテスト失敗が表示された。実際は全スイート green。原因は (6) セッションID「#3156 failed」を件数と誤読、(7) advisory チェックの結果を報告する見出し「（機械判定 acceptance: ❌ 1件）」を検証者の判定と誤読。
- **原則3（1日1変数）より原則2（計測を先に直す）を優先**した。§4-4 は仕様文書の整合で計測を変えないため、交絡は無い。31件コーパスで反転4件（718/506/558/588、全て advisory 報告行）、27件据え置き。
- 副次的発見: 否定形の受入基準「〜は変更しない」は advisory acceptance のトークン照合で必ず不一致になる（対応する変更ファイルが存在しないため）。**受入基準は肯定形で書く**。私が #718 に書いた基準3が引き金。
- 遠因として、私が回答文に「session #3156 failed」と ID と状態語を並べて書き、検証者がそれを転記した。以後 ID と状態語を隣接させない。
- 反映にはバックエンド再起動が要る（primary の HEAD は更新済みなので、auto-run dry 時の自動再起動で載る）。

### 17:0x L3（3件目）— 誠実性ゲート 8 形目（#710）

- #710 が同一行「❌ 実施せず（監督者訂正により撤回）」で **3 回連続**差し戻し（原則「3回目まで観測」を満たしてから介入）。承認済み plan が編集不能なため、撤回した項目を検証者は ❌ で書くしかない。「撤回」明示行のみ除外（「実施せず」単独は本物の未実装と同じ表記なので除外しない）。31件コーパスで反転 710 のみ。
- 構造的な根: 承認済み plan.md に監督者の判断を反映する経路が無い（#674/#718/#710 で 3 度目）。提案 3「監督者メモ／plan 追記」の優先度を上げる。

### 17:1x #710 4回目 — 差分レビューの正当な指摘、ハーネス衛生の穴

- 再起動後、#710 は誠実性ゲートを通過（8形目の修正が実地で効いた）。4回目の差し戻しは差分レビュー: `.agent-pids/cli-agent-*.pid` 2件が差分に混入（受入基準1〜3の実装自体は正しいと判定）。
- 根: `agent-process-tracker` は `process.cwd()/.agent-pids` に書くが、ルート `.gitignore` に無く（`rapitas-backend/.gitignore` のみ）、worktree ルートの `.agent-pids/` が untracked のまま `git add -A` に拾われた。ルート `.gitignore` に追加（`aa76fbb0`）。訓練信号とは無関係のリポジトリ衛生。#710 の自己修復（pid 除去）は観測する。
- #718 は再起動後 `reconciler_requeue` で再キュー（正常）。

### 17:2x #710 完了（PR #511）— ただし pid ファイルが PR に残っていた

- #710 は5回目で verify_passed → PR #511。しかし PR には `.agent-pids/*.pid` 2件が**残ったまま**だった。陪審の内訳: 4回目 claude pass / gemini fail(35) / chatgpt unknown → fail。5回目 claude pass(15) / gemini pass(5) / **chatgpt fail(45)** → 集計 pass。4回目に指摘した gemini が5回目は通し、唯一 fail を出した chatgpt が多数決で負けた。**実在する欠陥（計画外アーティファクト）が 1/3 票で通過**。
- 監督者が PR ブランチ（`bugfix/t710-update-task`）から `git rm --cached` で除去し push（`ee1215c8`、PR 内 0 件）。完了済みタスクのブランチで並行編集の危険が無いため実施。develop に実行時ファイルを恒久的に混入させないための最小対処で、実装の肩代わりではない。
- L0 観測: 陪審集計が「具体的なファイル名を伴う fail」を多数決で覆せる。判定精度の課題として記録。今日は介入しない（L3 を既に 3 件 + 衛生 1 件投入済み）。

### 23:1x〜23:4x 障害 — ゴーストソケット（2日連続）

- 23:12:46 の自動再起動（dev-restart）後、新バックエンド 24436 は Listen したが、外部接続は死んだ旧バックエンド 7184 の Listen ソケットに吸われ、UI の /tasks が 13 分以上タイムアウト（ユーザー報告「タスクが表示されない」）。3001 の状態: Listen 7184 ×1 / Established 7184 ×11 / CloseWait 7184 ×50。
- ハンドル保持者: **`bun test --isolate` が 2 プロセス（20:01 / 20:03 起動、各 1,385 ハンドル）3 時間以上ハング**。旧バックエンド配下の検証ゲート（関連テスト実行）が起動したフロントエンドのテストランナーで、親の Listen ソケットを継承したまま孤児化していた。7184 の子孫としては見えず（中間プロセスが消えていたため）、StartTime と Handles の異常値で特定。両者を kill → 即時復旧（health 200、tasks 14ms）。
- 昨日（8/27）の同型障害は再起動で回避したが、今回は保持者を特定して殺す経路が実証できた。**手順: `Get-Process | ? StartTime < 再起動時刻` で Handles が突出したプロセスを探す。**
- 監督者自身の掃除: 08-27/28 に Monitor が残した `watch-retry.sh` ループ 6 本を kill（現行の `sup-watch.py` は保持）。
- primary の WIP 5 ファイルは stash@{0} + `C:\Projects\primary-wip-2026-08-27.patch` に退避し、primary を origin/develop に揃えた（運用者承認）。以後は primary から直接 push。
- 運用者要望を起票: #7394（質問に推奨＋根拠を必須化）、#7541（一定時間無応答なら推奨を自動採用。ゲート条件を変える選択肢は対象外、1 タスク 1 回まで）。
- L3 候補（次サイクル）: 検証ゲートのテストランナーに**ハードタイムアウト**が無い／子プロセスがサーバーの Listen ソケットを継承する（spawn 時の handle inheritance）。どちらか一方で再発は防げる。

### 00:0x (08-30) primary 分岐の後始末 — 重複コミットを抱えたタスクブランチ

- #731 の差し戻し 4 回は「検証者の正直な失敗申告」だった。実装（worktree-guard の TTL キャッシュ陳腐化修正＋回帰テスト）は完成しており、落ちていたのは **tamper HARD ゲート**: ブランチが primary の旧ローカル develop から切られ、監督者の 13 コミット（保護パス `phase-output-validator.ts` を含む）を重複 SHA で抱えていたため「計画外の保護パス変更」と判定された。**監督者の運用（primary にコミット＋origin へ cherry-pick）が生んだ誤検知。**
- 該当ブランチは 2 本（`git cherry origin/develop <branch>` で重複>0）: #722（awaiting_question、即 rebase → 重複0）、#731（実装者実行中、終了後に rebase 予定）。
- 教訓: primary と origin の履歴が分岐している間に切られたブランチは、tamper ゲートと base 同期の両方で誤検知を起こす。**primary は常に origin と一致させ、push は直接行う。** cherry-pick 迂回は禁止。
- #731: worktree 内 rebase（重複13→0、未コミット差分2件は autostash で無傷）後の 6 回目で verify_passed → **PR #522**。5 回の差し戻しはすべて監督者の分岐由来の tamper 誤検知で、実装自体は 1 回目から完成していた。#722 も rebase 済み（awaiting_question のまま）。

### 02:3x (08-30) #715 — L1（基準の訂正）で再開、blocked 0 件

- #715「状態不整合: status=todo のまま workflowStatus 前進」の対象 #602 は、**監督者が 08-28 に retry したテーマ25（自動実行 enabled=false）のタスク**。`retry` は status=todo + フェーズ維持でディスパッチを待つ設計で、自動実行が無効だと永遠に待機 → Pattern B の猶予切れ後に必ず起票される（#706/#709/#715 の3件がこれ、現在も #188/#646/#647 が同形）。
- 直すべきデータは無く、根は**検出器がテーマの自動実行状態を見ていない**こと（`TriStateDesyncInput` に該当入力が無い）。旧基準「実データを整合させる」を撤回し、「自動実行が無効なテーマでは Pattern B を検出しない（有効なら従来どおり）」に書き直して retry（修復予算リセット）。#718 と同じ検出専用の設計原則を指示。
- 教訓: **自動実行が無効なテーマのタスクを retry しない**（待機状態が誤検出の種になる）。#646/#647 は据え置き（テーマ25 の有効化は運用者判断）。
- L3 候補（確定）: 変更なし完了が PR 経路に入る（#707/#735 ほか 3 回目）。汎用 rateLimitMiddleware は未マウントの死コード（x-forwarded-for || 'unknown' キーで有効化厳禁）。`[WorkflowRunner] Already running` が毎分 WARN（冪等 no-op のログ、ノイズ）。run-verification / dry-run の in-flight 429 はログに残らず計測不能。

### 03:0x (08-30) #722 — 前提不成立アイデアへの回答（L1）

- 「優先度ベースの営業時間連携修復スケジューリング」（idea #7358）は研究の前提監査で 5 仮定中 4 件が不成立（チーム/HR連携/時間帯別実測/分散TZ）。実装フェーズ発の質問に **推奨 A（却下・コード変更なし）** で回答。B（CI 修復の即時/延期に矮小化）は延期を裏付ける実測が無く、固定継続方針にも触れるため不採用。
- 手順: question.md に回答を追記 → `resume-from-question`（previousStatus=research_done へ復帰、成果物保持）。#718 の教訓どおり `answer-question` は使わない。
- 観測: イノベーション起票がツールの前提（単一開発者・ローカル運用）を無視したアイデアを生成した例。7 日間で同型は本件のみ（0 件）。反復すれば L1 でイノベーションジョブへ前提を与える。
- 03:07 #722 が誠実性ゲートで差し戻し（9形目）: verify.md が research の**前提監査**を家の書式どおり「❌ 不成立」で引用 → 失敗信号と誤読。**ゲートには手を入れない**（今日3件目の regex 継ぎ足しは打ち切り、構造的な判定ブロック化を次フェーズ最優先とする根拠に追加）。L1: 基準を「前提不成立の記録／差分0／修正不要判定」の3件に書き換え（予算リセット）、question.md に「記号を使わず引用」の指示を追記。
- 03:09 #722 の実装者が、差し戻しを受けて**同じ Q1 を question.md に再保存**し再び awaiting_question に（回答は本文に既にあった）。`resume-from-question` で in_progress へ復帰。
- 観測（L3 候補）: `theme-auto-run-scheduler` が awaiting_question のタスクを「queue item 無し」として ~12 秒毎に再投入し、runner が「awaiting_question では実行不可」で cancel、を 6 回繰り返してから "keeps being cancelled — releasing it" で自己解除。実害は軽微だが、スケジューラの再投入条件に awaiting_question を除外すべき。

### 03:3x 運用者ルール — 停滞したらログを見て介入判断

- 「一定時間進まないタスクはログを確認して介入要否を判断する」を運用ルールとして採用。監督ウォッチャーに stall 検知を追加（非終端タスクで最終遷移から 20 分、awaiting_question は 15 分、同一タスク 60 分に 1 回）。判断手順（live 実行 → キュー → 質問 → ERROR ログ）はメモリ `stall-check-before-intervene` に記録。

### 03:4x 初の [stall] — #722（L0、自己修復を待つ）

- #722 が in_progress のまま 20 分無遷移。実測: 実行なし・キュー項目なし（最新セッション interrupted 03:10、実行 03:09 完了）。`resume-from-question`（03:1x）は workflowStatus を戻すだけで**キューに積まない**。直前にスケジューラが awaiting_question の #722 を ~12 秒毎に再投入→runner が cancel、を 6 回繰り返し "releasing it" で手放していたため、復帰後に誰も積まなかった。
- リコンサイラ `requeueOrphanTasks` は `updatedAt < now-45min` を条件に status=todo へ戻す（60 秒周期、上限 2 回）。03:1x 更新なので **03:58 頃に自動再投入**される見込み → 介入せず観測。
- L3 候補: (1) `resume-from-question` は復帰時にキュー項目を作る（または scheduler に即時通知）、(2) `theme-auto-run-scheduler` の再投入条件から awaiting_question を除外。両方とも「回答後 45 分の空転」を消す。
