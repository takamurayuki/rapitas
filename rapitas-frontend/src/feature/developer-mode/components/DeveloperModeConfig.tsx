/**
 * DeveloperModeConfig
 *
 * Backward-compatibility re-export shim.
 * The implementation has been split into sub-components under
 * ./developer-mode-config/. Import from there for new code.
 */

// NOTE: Re-exported to preserve all existing import paths across the codebase.
export { DeveloperModeConfigModal } from './developer-mode-config';
