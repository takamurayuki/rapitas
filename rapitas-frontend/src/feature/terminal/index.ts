/**
 * terminal — public API barrel for the integrated terminal feature.
 */
export { default as TerminalProvider } from './TerminalProvider';
export { useTaskTerminalContext } from './useTaskTerminalContext';
export { useTerminalContextStore } from './terminal-context-store';
export { useTerminalStore } from './terminal-store';
