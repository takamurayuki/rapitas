/**
 * MemoSection (re-export shim)
 *
 * Backward-compatibility re-export so that existing imports of
 * '@/feature/tasks/components/MemoSection' continue to resolve without changes.
 * The actual implementation lives in ./memo-section/.
 */

export { default } from './memo-section/MemoSection';
