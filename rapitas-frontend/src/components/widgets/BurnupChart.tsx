/**
 * BurnupChart (legacy path)
 *
 * Compatibility re-export — the widget now lives in ./burnup-chart/ (split per
 * COMPONENT_SPLITTING_POLICY). New code should import from
 * '@/components/widgets/burnup-chart' directly.
 */
export { default } from './burnup-chart';
