/**
 * tasks/new index
 *
 * Barrel re-export for the new-task page module.
 * Consumers that previously imported NewTaskClient directly continue to work unchanged.
 */
export { default } from './NewTaskClient';
export { default as NewTaskClient } from './NewTaskClient';
