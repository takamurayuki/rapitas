/**
 * HomeClient
 *
 * Re-export shim — the implementation has been moved to the home/ directory.
 * This file exists to preserve existing imports (e.g. page.tsx) without change.
 */
export { default } from './home';
