/**
 * Temporary Statistics Routes
 *
 * Placeholder for temporary/experimental statistics endpoints.
 * Kept as a stub to satisfy the route barrel export.
 */
import { Elysia } from 'elysia';

export const tempStatisticsRoutes = new Elysia({ prefix: '/temp-statistics' });
